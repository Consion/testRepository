什么是代理模式(What)：为其它对象提供一种代理以控制对这个对象的访问。
代理模式目标(Why)：为了对目标对象的方法进行增强，这种增强的本质就是对目标方法进行拦截和过滤。
代理模式如何使用（How）：详见代码。
代理的分类
    1.透明代理，用户不知道有代理的存在。（只能通过代理进行访问）
    2.普通代理，用户需要手动设置代理，然后才能进行访问。（只能通过代理进行访问）
      调用者只知道代理而不用知道真实的角色是谁，屏蔽了真实角色的变更对高层模块的影响  通过在真实对象中传递代理类对象来限制new真实对象。
    3.强制代理，则是调用者调用真是对象进行访问，而不用关心代理是否存在，因为真实对象内部还是通过代理对象访问。
    4.动态代理，在实现阶不用关心代理谁，而在运行阶段才指定代理哪一个对象。