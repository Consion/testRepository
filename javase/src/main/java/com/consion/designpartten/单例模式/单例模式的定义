定义：确保某一个类只有一个实例，而且自行实例化并向整个系统提供这个实例

优势：创建对象消耗资源大的情况下可以考虑单例模式，可以在应用启动时直接产生一个单例对象
一直贮存在内存中

缺点：扩展困难，不利于测试，没有实现接口也无法使用mock方式来测试。

应用场景：
    1.要求生成唯一序列号的环境
    2.在整个项目中需要一个共享访问点或共享数据，如web页面上的计数器，可以先缓存到单例对象中，前提是
      要确保线程安全。
    3.创建一个对象需要消耗资源过多，如IO操作和数据库操作